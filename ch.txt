node app.js


// 1. Подключаем модуль express
const express = require('express');

// 2. Создаем экземпляр приложения (наш сервер)
const app = express();

// 3. Определяем обработчик для GET-запроса на корневой маршрут ('/')
app.get('/', (req, res) => {
  // Когда пользователь заходит на "http://localhost:3000/", сервер отправляет эту строку
  res.send('Hello World');
});

// 4. Запускаем сервер на порту 3000
app.listen(3001, () => {
  console.log('Сервер запущен по адресу http://localhost:3001');
});

lsof -i :3001
pkill -f node


Запуск проекта:
npm install
npm start

вот jsonдля простого запуска проекта
{
  "name": "shcherbak_maxim_lb2",
  "type": "module",
  "version": "1.0.0",
  "description": "Лабораторная работа по WEB - Библиотека книг",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "dev": "node app.js"
  },
  "dependencies": {
    "cookie-parser": "^1.4.7",
    "express": "^4.21.2",
    "express-fileupload": "^1.5.2",
    "pug": "^3.0.2"
  },
  "keywords": [
    "express",
    "pug",
    "library",
    "books"
  ],
  "author": "Shcherbak Maxim",
  "license": "MIT"
}



Запуск приложения:
bash
# Удаляем node_modules если они есть
rm -rf node_modules

# Запускаем через Docker Compose
docker-compose up --build

# Или через npm скрипт
npm run docker:up
Остановка приложения:
bash
docker-compose down


удалить  docker system prune -f
